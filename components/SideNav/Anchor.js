import styled from "styled-components";

const Anchor = styled.a`
  text-decoration: none;
  color: inherit;

  $:link {
    text-decoration: none;
    color: inherit;
  }

  $:visited {
    text-decoration: none;
    color: inherit;
  }

  $:hover {
    text-decoration: none;
    color: inherit;
  }

  $:active {
    text-decoration: none;
    color: inherit;
  }
`

export default Anchor
